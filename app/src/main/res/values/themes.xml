<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.GithubProfile" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.GithubProfile" parent="Base.Theme.GithubProfile" />

    <style name="Theme.GithubProfile.AppSplash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/base_splashscreen</item>
    </style>
</resources>